import styled from "styled-components";

export const ErrorContainer = styled.div`
  padding: 2rem;
  display: flex;
  align-items: center;
  flex-flow: column;
  background-color: rgba(245, 0, 3, 0.2);
  border-radius: 5px;
`;

export const TextDetail = styled.div`
  margin-top: 1rem;
  font-size: 1.5rem;
  font-weight: bold;
  font-style: italic;
`;
